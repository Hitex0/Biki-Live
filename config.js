module.exports = {
    token: "process.env.BIKI_GG",
    channel: "process.env.BIKI_YOUTUBE",
    messageTemplate: "Hello @everyone, **{author}** just now uploaded a video! Go check it out **{title}**!\n{url}",
    channel_id: "process.env.BIKI_YT",
    watchInterval: 3000
}
